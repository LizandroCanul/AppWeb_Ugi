import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

@NgModule({
  declarations: [],
  imports: [
    BrowserModule,
    // otros módulos
  ],
  providers: [],
  bootstrap: []  // Si usas un componente standalone, aquí no se incluirá
})
export class AppModule { }
